{% extends 'base.html.twig' %}

{% block title %}Edit Annonce{% endblock %}

{% block body %}
    <h1 class="text-center mt-5">Mettre à jour l'annonce</h1>

    {{ include('annonce/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="text-center mt-4">
        <a href="{{ path('app_annonce_index') }}" class="btn btn-primary">Retour à la liste</a>
    </div>
{% endblock %}
